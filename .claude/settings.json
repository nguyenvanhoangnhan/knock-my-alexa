{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "cmd=$(jq -r '.tool_input.command // \"\"'); case \"$cmd\" in *git*push*) \"$CLAUDE_PROJECT_DIR/scripts/check-secrets.sh\" --tracked >&2 || exit 2 ;; *git*commit*) \"$CLAUDE_PROJECT_DIR/scripts/check-secrets.sh\" --staged >&2 || exit 2 ;; esac",
            "timeout": 30,
            "statusMessage": "Scanning for secrets"
          }
        ]
      }
    ]
  }
}
